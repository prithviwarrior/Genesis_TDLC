#ifndef __CALC_H
#define __CALC_H

#include <stdio.h>
#include <math.h>

int add(int a, int b);
int sub(int a, int b);
int multi(int a, int b);

int dollar(int a);
int rupees(int a);
//int Dec(hex);

#endif
